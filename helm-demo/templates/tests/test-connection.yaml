apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "helm-demo.fullname" . }}-test-connection"
  labels:
    {{- include "helm-demo.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "helm-demo.fullname" . }}:8080']
  restartPolicy: Never
